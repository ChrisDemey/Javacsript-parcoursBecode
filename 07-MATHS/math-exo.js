/* minimum et maximum */
let min= Math.min;
Math.min(7, 5, -12, 6, 32, 18, 14, -2);
let max= Math.max;
Math.max(-3, 9, 21, 36, 27, 54, 17, 35);
console.log (min + max);

/* Aléatoire 1 */
let floatBateau = 10.4;
let couleBateau = (Math.floor(floatBateau));
let voleBateau = (Math.floor(floatBateau));

/* Aléatoire 2 */
let random = (Math.random(50, 100));
console.log (random);

/* Aléatoire 3 */
let persos = ["Goro","Johnny Cago","Kano","Liu Kano","Raiden","Reptil","Scorpion","ShangTsun","Sonya","Sub-Zero"];
Math.random(persos);
console.log(Math.random);
